{
  "name": "work-engine",
  "version": "1.0.0",
  "description": "Project & task management engine for Obsidian vault — MOC-driven work tracking",
  "type": "module",
  "bin": { "work-engine": "./dist/index.js" },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "gray-matter": "^4.0.3",
    "glob": "^10.3.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0",
    "vitest": "^1.0.0"
  }
}
